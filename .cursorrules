Your permissions for json files in data is reading is allowed but not writing

Avoid using mocks in tests, unless there is no other option

Be concise, make incremental changes and test before proceding.

Don't try to fix everything at once.

Avoid changing the schema unless instructured to do so

e2e tests should use production data, other tests should use test data

use `test` instead of just vitest

run cli with tsx

prioritize fixing tests in the following order: unit, functional, e2e
